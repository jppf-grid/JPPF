apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jppf-driver.fullname" . }}-test-connection"
  labels:
{{ include "jppf-driver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jppf-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
