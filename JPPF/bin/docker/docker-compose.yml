version: "3"

services:
  jppf-driver:
    image: "jppf-driver:${JPPF_VERSION}"
    env_file:
    - .env
    ports:
      - "11111:11111"
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "2"
          memory: 512M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 15s
    networks:
      jppf_net:

  jppf-node:
    image: "jppf-node:${JPPF_VERSION}"
    env_file:
    - .env
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "1"
          memory: 512M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 15s
    networks:
      jppf_net:

networks:
  jppf_net:
    ipam:
      driver: default
      config:
        - subnet: "${JPPF_SUBNET}"
