SSL configuration, keys and certificates for one-way authentication

The driver key store contains the driver private key
The driver has no trust store

The client and node have no key store
The client and node trust store contains the driver certificate